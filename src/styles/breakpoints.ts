type Breakpoints = {
  sm?: string;
  md?: string;
  lg?: string;
  xl?: string;
  xxl?: string;
};

const breakpoints: Breakpoints = {
  sm: "641px",
  md: "769px",
  lg: "1025px",
  xl: "1281px",
  xxl: "1441px",
};

export default breakpoints;
