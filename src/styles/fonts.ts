export type Fonts = {
  body: string;
  heading: string;
};

const fonts: Fonts = {
  body: `"Montserrat Variable", sans-serif`,
  heading: `"Space Grotesk Variable", sans-serif`,
};

export default fonts;
