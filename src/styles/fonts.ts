export type Fonts = {
  body: string;
  heading: string;
};

const fonts: Fonts = {
  body: `"Satoshi Variable", sans-serif`,
  heading: `"Clash Display Variable", sans-serif`,
};

export default fonts;
